<% provide(:title, "Home") %>
<%= will_paginate @feed_items,class:"pagenation-parent",:previous_label => ' &lt Back', :next_label => 'Next &gt' %>
  <ul class="content list-group">
      <% @feed_items.each do |image| %>
         <%= render 'like_form',    image: image %>
         <%= render "images/image", object: image %>
      <% end %>

<%= will_paginate @feed_items,class:"pagenation-parent",:previous_label => ' &lt Back', :next_label => 'Next &gt' %>
  </ul>

